version: "3"

services:
  homer:
    image: phntxx/dashboard:latest
    container_name: dashboard
    ports:
      - 8010:8080
    volumes:
      - /opt/dashboard:/app/data
    restart: unless-stopped
