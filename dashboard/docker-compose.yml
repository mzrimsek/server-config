version: "3"

services:
  homer:
    image: b4bz/homer:latest
    container_name: homer
    ports:
      - 8010:8080
    volumes:
      - /media/share/homer/assets:/www/assets
      - /media/share/homer/config.yml:/www/assets/config.yml
    restart: unless-stopped
